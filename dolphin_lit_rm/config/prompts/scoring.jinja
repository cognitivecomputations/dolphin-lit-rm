You are an expert literary critic.  
Evaluate the text against each metric and **return a single JSON object** whose
property names are the *metric names* shown below and whose values are the
numeric scores (0.0 – 1.0).  
No prose, no markdown – just the JSON.

TEXT:
"""
{{ response }}
"""

Your task is to score the above text on the following criteria on a 0-1 scale:
{% for metric in metrics %}
{{ metric.name }} — {{ metric.description }} (Hint: {{ metric.prompt_hint }})
{% endfor %}

---  

Note: if a criterion doesn't apply to the text, omit it from the output.

Return **only** a JSON object like:

{
{% for metric in metrics %}
  "{{ metric.name }}": <float between 0.0 and 1.0>{% if not loop.last %},{% endif %}
{% endfor %}
}
